global:
  imageRegistry: ""
  imagePullSecrets: []
  storageClass: ""

replicaCount: 1

image:
  registry: ghcr.io
  repository: docspec/api
  tag: "1.21.2"
  digest: ""
  pullPolicy: IfNotPresent
  pullSecrets: []

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  name: ""
  annotations: {}
  automountServiceAccountToken: false

podAnnotations: {}
podLabels: {}
commonLabels: {}
commonAnnotations: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
  fsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

containerSecurityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

containerPorts:
  http: 4000

service:
  type: ClusterIP
  port: 4000

ingress:
  enabled: false
  className: ""
  annotations: {}
  hostname: api.docspec.local
  path: /
  pathType: Prefix
  tls: false

# Resource preset from common chart: nano/micro/small/medium/large/xlarge/2xlarge
# When autoscaling is enabled, resource requests MUST be set for HPA to function
resourcesPreset: "small"
resources: {}

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: ""

podDisruptionBudget:
  enabled: true
  minAvailable: ""
  maxUnavailable: 1

networkPolicy:
  enabled: true
  allowExternal: true
  allowExternalEgress: false
  extraIngress: []
  extraEgress: []
  ingressNSMatchLabels: {}
  ingressNSPodMatchLabels: {}

livenessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 10
  periodSeconds: 30
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

# /metrics has NO auth and shares the API port. Restrict via networkPolicy
# (source-scoped) or a service-mesh policy. Do NOT expose it via Ingress.
metrics:
  enabled: true
  path: /metrics
  # Empty = fall back to containerPorts.http. Applies to the prometheus.io/port
  # pod annotation only — ServiceMonitor uses the Service's named 'http' port.
  port: ""
  # Vanilla Prometheus kubernetes_sd_configs pod discovery.
  podAnnotations:
    enabled: false
  # Prometheus Operator CRD. Requires monitoring.coreos.com/v1 in the cluster.
  # Off by default: enabling on a cluster without the CRD hard-fails helm install.
  serviceMonitor:
    enabled: false
    interval: 30s
    scrapeTimeout: 10s
    honorLabels: false
    scheme: http
    labels: {}
    namespaceSelector: {}
    relabelings: []
    metricRelabelings: []

nodeSelector: {}
tolerations: []
affinity: {}

extraEnvVars: []
extraVolumes: []
extraVolumeMounts: []
